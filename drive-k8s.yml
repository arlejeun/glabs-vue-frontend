apiVersion: apps/v1
kind: Deployment
metadata:
  name: drive
  namespace: drive
spec:
  replicas: 1
  selector:
    matchLabels:
      name: drive
  template:
    metadata:
      labels:
        name: drive
    spec:
      containers:
        - name: drive
          image: us-west1-docker.pkg.dev/techacc-platform-dev/drive/drive-web:development
          imagePullPolicy: Always
          volumeMounts:
          - mountPath: "/mnt/certs/"
            name: wildcard-demo-genesys-com-2022
            readOnly: true
          ports:
            - containerPort: 80
      imagePullSecrets:
        - name: pullsecret
      volumes:
        - name: wildcard-demo-genesys-com-2022
          secret:
            secretName: wildcard-demo-genesys-com-2022

---

apiVersion: v1
kind: Service
metadata:
  name: drive
  namespace: drive
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    name: drive

---

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: drive
  namespace: drive
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "0"
    kubernetes.io/ingress.class: "nginx"
spec:
  tls:
  - hosts:
    - drive.demo.genesys.com
    secretName: wildcard-demo-genesys-com-2022
  rules:
  - host: drive.demo.genesys.com
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: drive
            port: 
              number: 80